{
  "$id": "https://gcn.nasa.gov/schema/main/gcn/notices/core/Classification.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "version": "0.7.0",
  "title": "Classification",
  "description": "Source classification with optional subtype and confidence.",
  "properties": {
    "classification": {
      "description": "Canonical class name from skyportal/timedomain-taxonomy (e.g., 'Ia', 'Ic-BL', 'Tidal Disruption Event', 'kilonova').",
      "title": "Classification",
      "type": "string"
    },
    "subtype": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Free-form subtype refinement when the taxonomy lacks a finer class.",
      "title": "Subtype"
    },
    "confidence": {
      "anyOf": [
        {
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Classifier confidence [0-1] when reported.",
      "title": "Confidence"
    },
    "taxonomy_path": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Root-to-leaf hierarchy path for `classification` in the time-domain taxonomy (e.g., ['Time-domain Source', 'Stellar variable', 'Cataclysmic', 'Supernova', 'Type I', 'Ia']). Auto-populated from `classification`; lets a downstream consumer collapse to a coarser campaign class without re-walking the taxonomy.",
      "title": "Taxonomy Path"
    }
  },
  "required": [
    "classification"
  ],
  "type": "object"
}
